import { Router } from "express";

const router = Router();

// router.post(
//   "/getmails-messages/history",
//   async (req, res) => {
//     uploadExcel(req, res);
//   }
// );

export default router;
